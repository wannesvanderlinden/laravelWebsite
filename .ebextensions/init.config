container_commands:
    01initdb:
        command: 
            "php artisan migrate"
    02initdb:
        command:
            "php artisan route:clear"
    03initdb:
        command:
            "php artisan cache:clear"
    04initdb:
        command:
            "php artisan db:seed"